security.ignored: /**

spring:
  jackson:
    defaultPropertyInclusion: NON_NULL
  application:
    name: booking
  jpa:
    show-sql: true
    hibernate.ddl-auto: validate

logging:
  level:
    com.netflix.discovery: 'OFF'